import { CustomPaginationStyleDirective } from './custom-pagination-style.directive';

describe('CustomPaginationStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomPaginationStyleDirective();
    expect(directive).toBeTruthy();
  });
});
